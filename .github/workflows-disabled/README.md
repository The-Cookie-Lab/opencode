Disabled upstream workflows retained for reference.

These workflows are not active because GitHub only loads workflow files from
`.github/workflows/`. They may be useful later if the fork chooses to restore
container publishing, generated-code writeback, Nix hash writeback, or browser
e2e coverage with fork-owned secrets, runner policy, and stability budgets.
